# AWS Environment Variables

The backend is configured to run in AWS using environment variables.

## Required

- ASPNETCORE_ENVIRONMENT=Production
- CONNECTIONSTRINGS__DEFAULT=postgres connection string

## Optional / Future

- AWS_REGION
- AWS_S3_BUCKET_NAME
- APPLICATION__NAME
